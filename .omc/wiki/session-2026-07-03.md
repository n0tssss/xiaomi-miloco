---
title: 2026-07-03/04 Session: PR #279 → 推荐架构 主线 #1 落地
category: session-log
tags: [session, refactor, pr-hermes, hermes-pr-md, doc-mainline, adapter]
updated: 2026-07-04
---

# 2026-07-03/04 Session — PR #279 主线 #1 (backend Adapter + plugin HermesAdapter) 落地

> 续 [[session-2026-07-03]] 末尾的"主线重构 #1 部分落地"。本 session 完成全部 #1 工作并跑通基础 e2e。

## 本 session 完成

### 主线 #1 backend + plugin 改造(8 个 commit on pr-hermes)

| commit | 内容 |
|---|---|
| `4a83d74` | fix(install-hermes): Step 1.10 探能 import miloco 的 python(MILOCO_PY 之前没定义) |
| `1c1681c` | fix(install-hermes): Step 1.10 cp 列表加 miot/client.py(perception-toggles v2 配套) |
| `a15c51e` | **feat(pr-hermes): backend AgentPlatformAdapter + HermesAdapter 接入**(主线 #1 主体,957 行变更) |
| `67c9c8e` | fix(install-hermes): Step 1.10 cp 列表加 agent_platform/(backend wheel 缺) |
| `67c9c8e`... | fix(install-hermes): Step 1.10 cp 列表加 config/settings.py(加 platform 字段) |

integration HEAD = `fcd94a0`(4 个 merge commit)

### 新增/修改文件
- backend 新增:`miloco/agent_platform/{__init__.py, base.py, loader.py}` (366 行)
  - `AgentPlatformAdapter` ABC + `TurnContext` / `AgentTurnResult` / `TraceMeta` 数据类
  - duck-typed loader(从 `$MILOCO_HOME/agent_platform/<name>/adapter.py` importlib 加载,plugin 不依赖 backend wheel)
  - `AdapterTransportError` / `AdapterTransientError` 异常类
- backend 修改:`config/settings.py`(加 `AgentSettings.platform` 字段)、`dispatch/dispatcher.py`(加 `_send_via_adapter` + 优先 adapter / webhook fallback)
- plugin 新增:`miloco-plugin/hermes_adapter/{__init__.py, adapter.py}` (420 行)
  - `Adapter` 类:OpenAI `/v1/chat/completions` POST + `<system>` 消息注入(对齐 doc #2)
  - 溢出自愈:沿用 279 best-effort(无 session 头重试)
  - `build_system(profile, extra)`:复用 `_build_prepend` / `_build_append`(对齐 `context_injection`)
  - `read_trace_meta(run_id)`:**stub**(本 session 未重构 `trace.py`,通常返回 None)
- plugin 修改:`install-hermes.sh` 加 Step 4.8 + Step 1.10 cp 列表补 4 个文件

### 验证结果
- ✅ Backend 启动 + `/health` 200(PID 87334 → 88304)
- ✅ Adapter load:`agent adapter loaded: name=hermes class=Adapter dir=/Users/wkea/.openclaw/miloco/agent_platform/hermes`
- ✅ `build_system('full')` 返 4073 字符、`build_system('minimal')` 返 0 字符
- ✅ `device catalog` 自动 refresh (1321 chars)
- ✅ `--diagnose` 14/14 全过
- ✅ `hermes chat -q "ping" -Q` 返 "pong. 小坚果在,链路通."
- ✅ 4 个 miloco cron jobs 注册并跑 ok(01:15:30 last ok / 01:00:40 last ok)

## 仍未完成(下 session 计划)

### 主线剩余
1. **【#2 完整迁移】** `context_injection.py` 重构:删 `pre_llm_call` 注册,完整迁移 `build_prepend` / `build_append` 到 HermesAdapter.build_system
2. **【#11 trace 迁移】** `trace.py`:去掉 debug 门槛,常写 `$MILOCO_HOME/trace/agent/*.meta.json`
3. **【#11 trace 迁移】** `agent_meta_poller.py`:改读盘(目前仍走 webhook get_trace)
4. **【#1 完成】** 删 `plugins/hermes/adapter/`(整个独立进程栈):server.py / hermes_client.py / session_map.py / gateway_watch.py / trace_reader.py / __main__.py
5. **【#7 配置收敛】** 改 `miloco-cli config set agent.webhook_url` 走 CLI,不直接 patch config.json

### 独立收敛项(doc §五 #3-#10)
6. **【#3 裁 tool】** 删 `miloco_status` / `miloco_test_push`,保留 im_push / notify_bind / habit_suggest
7. **【#4 im_push 对齐 OpenClaw】** DeliveryRouter + fallback + needsBind 两步握手
8. **【#5 清理 skill 平台耦合】**【miloco 团队】
9. **【#6 habit 下沉】**【miloco 团队】
10. **【#8 register 拉后端】** plugin 启动时 subprocess miloco-cli service restart
11. **【#9 自实现 miloco-cli memory search】**【miloco 团队】
12. **【#10 MILOCO_HOME 显式配置】** install 写 `MILOCO_HOME=~/.hermes/miloco`(替换默认 `~/.openclaw/miloco`)
13. **【#12 cron webhook 化】**【miloco 团队,非阻塞】

## 已知 onboarding_trigger 错(无关本主线)

`miloco.home_profile.onboarding_trigger` import `OnboardingKeys` 失败(fork 的 `database/kv_repo.py` 没这名字)。
**被 try/except 吞掉,backend 不挂**。若下 session 跑感知 onboarding 流程,需要修 fork 的 kv_repo.py 或 upstream onboarding_trigger。

## 当前架构(主线 #1 后)

```
┌─────────────────────────────────────────────────────────┐
│ miloco backend (FastAPI, uv tool venv)                  │
│                                                          │
│  ┌─────────────────┐                                     │
│  │ AgentDispatcher │ (dispatch/dispatcher.py)            │
│  │                 │                                     │
│  │ 1. try adapter  │ ─→ AgentPlatformAdapter.send_turn()│
│  │ 2. fallback     │     (loader.py importlib 动态加载)   │
│  │    webhook      │                                     │
│  └─────────────────┘                                     │
│         │  (loaded from $MILOCO_HOME/agent_platform/hermes) │
│         ▼                                                │
│  ┌─────────────────┐                                     │
│  │  HermesAdapter  │ (cp from plugins/hermes/.../hermes_adapter/) │
│  │                 │                                     │
│  │  build_system() │ ─→ context_injection._build_prepend │
│  │                 │     _build_append(legacy, plugin 副本)│
│  │  send_turn()    │ ─→ POST Hermes /v1/chat/completions │
│  │  read_trace_meta│ ─→ 读 $MILOCO_HOME/trace/*.meta.json│
│  └─────────────────┘     (本 session: stub, trace.py 未改) │
└─────────────────────────────────────────────────────────┘
                              │
                              ▼
                   Hermes api_server
                   (POST /v1/chat/completions,
                    X-Hermes-Session-Id 头保会话连续,
                    16 个 miloco-* skills 自动可见)
                              │
                              ▼
                       LLM (MiMo / 自配)
                              │
                              ▼
                       用户(IM + dashboard)
```

## 关联
- 参考:`/Users/wkea/project/hermes-pr.md`
- 改动总结:`/Users/wkea/Xiaomi-Miloco-Fork-改动总结-2026-07-03.md`
- 上一 session:[[session-2026-07-03]]
- 记忆页:[[fork-overview]] [[branch-discipline]] [[mac-dev-env]] [[hermes-install-pitfalls]]