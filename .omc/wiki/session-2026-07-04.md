---
title: 2026-07-04 Session: pr-hermes 主线 #1+#2+#11+#1 全部落地完成
category: session-log
tags: [session, refactor, pr-hermes, hermes-pr-md, doc-mainline, complete]
updated: 2026-07-04
---

# 2026-07-04 Session — `hermes-pr.md` 主线全部落地完成

> 续 [[session-2026-07-03]]。本 session 完成主线 #2 + #11 + #1 完成,共 4 个 feature commits,作者要求的 12 项中主线 3 项全部到位。

## 本 session 完成

### 备份基线
- `git tag v0.2-pr-hermes-mainline-1` — 主线 #1 节点备份
- `tar backup ~/miloco-integration-backup-*.tar.gz` — 增量备份(每阶段)

### 4 个 Feature Commits(全部 on pr-hermes)

| # | commit | 内容 |
|---|---|---|
| 1 | `a99cd8e` | **feat(pr-hermes): #2 主线 - 删 pre_llm_call + adapter URL/key 修正**<br>• plugin/__init__.py: 删 pre_llm_call 钩子注册<br>• hermes_adapter/adapter.py: 默认 URL 18100→8642 + API_SERVER_KEY 从 ~/.hermes/.env 兜底 |
| 2 | `bd17026` | fix(install-hermes): Step 1.10 cp settings.py + agent_meta_poller.py(补 cp) |
| 3 | `35c13cc` | **feat(pr-hermes): #11 主线 - trace disk IPC 迁移**<br>• trace.py: 去 debug gate(无条件落盘) + meta.json 平铺路径 + on_session_start 派生 trace_id<br>• agent_meta_poller.py: 优先 adapter.read_trace_meta,fallback webhook |
| 4 | `a436c47` | **feat(pr-hermes): #1 完成 - 删 plugin/adapter/ 独立进程栈**<br>• 删 `plugin/adapter/`(7 个文件: server / hermes_client / session_map / gateway_watch / trace_reader / __main__ / __init__)<br>• 删 `scripts/{miloco-adapter.sh, adapter-launcher.sh, com.xiaomi.miloco.hermes.adapter.plist}`<br>• 删相关测试(6 个)<br>• 删 UPGRADE.md<br>• install-hermes.sh: Step 4 不 cp adapter / Step 5 改写 platform / Step 7 deprecated + 清理 launchd 残留 / diagnose adapter launchd+health 改 N/A |
| 5 | `3a0ea35` | fix(install-hermes): 终态 banner 删 $ADAPTER_LOG 引用 |
| 6 | `13eaee7` | **feat(pr-hermes): dispatcher 去 webhook fallback(adapter-only)**<br>• _send_batch 直接调 adapter,无 webhook 分支<br>• adapter 缺失则 warn + drop(明确指引运维检查) |
| 7 | `cd7f168` | fix(install-hermes): Step 1.10 cp dispatcher.py |

integration HEAD = `0d0131e`(合并 4 个 feature commit)
git tags: `v0.3-pr-hermes-mainline-2`, `v0.4-pr-hermes-mainline-11`, `v0.5-pr-hermes-mainline-1-complete`

### 验证结果

| 项 | 状态 |
|---|---|
| Backend 启动 + `/health` 200 | ✅ PID 97635,uptime 702s |
| `hermes chat -q "ping"` | ✅ "pong, 小坚果在的, 啥事?" |
| Adapter 直调 `send_turn()` | ✅ status=ok, rtt=2.97s |
| `--diagnose` 14 项 | ✅ 14/14 全过(adapter launchd + /health 显示"N/A 新架构") |
| launchd 残留检查 | ✅ launchctl list 无 com.xiaomi.miloco.hermes.adapter |
| 端口 18789 残留 | ✅ lsof 无 LISTEN |
| git 拓扑清理 | ✅ pr-hermes linear,无 mutation on integration |

### 演进过程发现的问题(踩过的坑)

| 现象 | 修法 |
|---|---|
| adapter 默认 URL `18100` → 502 | 改 `8642`(hermes gateway 主端口) |
| `MILOCO_PY` unbound | 加 probe(config.json > uv tool > pyenv > $PYTHON) |
| Step 1.10 缺 cp → backend wheel 旧版 | 补 cp 列表(settings.py / agent_meta_poller.py / dispatcher.py / agent_platform/) |
| fork 的 filter.py 没 `select_active_camera_dids` | Step 1.10 cp 列表加 client.py(fork 兼容版) |
| onboarding_trigger import OnboardingKeys 失败(fork kv_repo 缺) | try/except 吞掉,backend 不挂(无关本主线) |
| **Git 拓扑反复错乱**:误 commit 到 integration 4 次 | 每次 reset + 重新 commit 到 pr-hermes(本 session 总结教训) |

## 当前架构(完全 `hermes-pr.md` 主线)

```
┌─────────────────────────────────────────────────────────┐
│ miloco backend (FastAPI, uv tool venv)                  │
│                                                          │
│  ┌─────────────────┐                                     │
│  │ AgentDispatcher │ (dispatch/dispatcher.py)            │
│  │                 │                                     │
│  │  only adapter   │ ─→ AgentPlatformAdapter.send_turn()│
│  │  (no webhook    │     【#1 完成 去 fallback】           │
│  │   fallback)     │                                     │
│  └─────────────────┘                                     │
│         │  (loaded from $MILOCO_HOME/agent_platform/hermes) │
│         ▼                                                │
│  ┌─────────────────┐                                     │
│  │  HermesAdapter  │ (cp from plugins/hermes/.../hermes_adapter/) │
│  │                 │                                     │
│  │  build_system() │ ─→ context_injection._build_prepend │
│  │                 │     _build_append(legacy, plugin 副本)│
│  │  send_turn()    │ ─→ POST Hermes /v1/chat/completions │
│  │  read_trace_meta│ ─→ 读 $MILOCO_HOME/trace/agent/*.meta.json│
│  │                 │     【#11 平铺路径】                  │
│  └─────────────────┘                                     │
│         │  (trace.py 在 hermes 进程,文件 IPC)             │
└─────────────────────────────────────────────────────────┘
                              │
                              ▼
                   Hermes api_server :8642
                   (POST /v1/chat/completions,
                    X-Hermes-Session-Id 头保会话连续,
                    16 个 miloco-* skills 自动可见)
                              │
                              ▼
                       LLM (MiMo/自配)
                              │
                              ▼
                       用户(IM + dashboard)
```

## 仍未完成(下 session 计划 · 独立项)

按 `hermes-pr.md` §五,主线已完成。剩 8 项独立收敛:

### plugin 域(本 session 可做)
- ⏭️ **#3 裁 tool** (删 `miloco_status` / `miloco_test_push`,保留 im_push / notify_bind / habit_suggest) — 6/14 项诊断精简
- ⏭️ **#4 im_push 对齐 OpenClaw** (DeliveryRouter + fallback + needsBind 两步握手)
- ⏭️ **#7 改 miloco-cli config set** (CLI 走,plugin 不碰 config.json 结构)
- ⏭️ **#8 register 内 subprocess miloco-cli service restart** (plugin 启动拉后端)

### miloco 团队域(用户明确不属本分支)
- ⏭️ #5 清理 skill 平台耦合内容
- ⏭️ #6 habit 状态机下沉 CLI
- ⏭️ #9 自实现 miloco-cli memory search
- ⏭️ #12 后端 cron 触发(替代 plugin cron)

### 文档/测试(本 session 可做)
- ⏭️ plugin/README.md 重写(旧文大量引用 standalone adapter)
- ⏭️ 安装指南 install-guide-hermes.md 重写(Step 6 改 adapter 重启为不需)
- ⏭️ 补 test_context_injection.py(确认 `_build_prepend` / `_build_append` 在 HermesAdapter 环境下工作)

## 关联
- 参考:`/Users/wkea/project/hermes-pr.md`
- 改动总结:`/Users/wkea/Xiaomi-Miloco-Fork-改动总结-2026-07-03.md`
- 上一 session:[[session-2026-07-03]]
- 记忆页:[[fork-overview]] [[branch-discipline]] [[mac-dev-env]] [[hermes-install-pitfalls]]